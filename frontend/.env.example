# Strapi CMS (server-side — never expose the token to the browser)
STRAPI_URL=http://localhost:1337
STRAPI_API_TOKEN=your-strapi-api-token

# Public base URL for Strapi media assets in the browser
PUBLIC_STRAPI_URL=http://localhost:1337
